<UserControl x:Name="userControl" x:Class="Right_Click_Commands.Views.CommandConfig"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid x:Name="grid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding ActualHeight, ElementName=grid, Mode=OneWay}"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid x:Name="ImgIcon" Margin="10" />
        <Label Content="{Binding Label, ElementName=userControl}" Grid.Column="1" Margin="10" FontSize="23" VerticalContentAlignment="Center"/>
    </Grid>
</UserControl>
